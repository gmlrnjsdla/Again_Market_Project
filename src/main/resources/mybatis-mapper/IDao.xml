<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="com.hsl.sns.dao.IDao">
	
	<insert id="joinMemberDao">
		INSERT INTO user_sns(id, pwd, name, birth, mail, phone, nick, profile, greet)  
		VALUES(#{param1},#{param2},#{param3},#{param4},#{param5},#{param6},#{param7},#{param8},#{param9})
	</insert>
	
	<select id="memberInfoDao" resultType="com.hsl.sns.dto.MemberDto">
		SELECT * FROM user_sns WHERE id=#{param1}
	</select>
	
	<select id="memberListDao" resultType="com.hsl.sns.dto.MemberDto">
		SELECT * FROM user_sns WHERE id NOT IN (#{param1})
	</select>
	
	
	
	
	
	
	
	<!-- 인덱스관련 -->
	<insert id="contentWriteContDao">
		INSERT INTO post (postidx, content, id) 
			VALUES (post_seq.nextval, #{param1}, #{param2})
	</insert>	
		
	<insert id="contentWritePicDao">	
		INSERT INTO posturl (postingimgurlidx, postidx, fileOriName, fileName,  fileExtension, fileUrl) 
			VALUES (postingurl_seq.nextval,#{param1},#{param2},#{param3},#{param4},#{param5})
	</insert>
	<select id="postInfoDao" resultType="com.hsl.sns.dto.PostDto">
		select * from post where id=#{param1} order by postidx desc
	</select>

</mapper>
	
	