<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="com.hsl.sns.dao.IDao">

	<insert id="followDao">
		INSERT INTO follow(followidx, followeeid , followerid) VALUES(follow_seq.nextval,#{param1},#{param2} )
	</insert>
	
	<select id="followCheckDao" resultType="int">
		SELECT count(*) FROM follow WHERE followeeid=#{param1} AND followerid=#{param2}
	</select>
	
	<select id="followCountDao" resultType="int">
		SELECT count(*) FROM follow WHERE followerid=#{param1}
	</select>
	
	<select id="followerCountDao" resultType="int">
		SELECT count(*) FROM follow WHERE followeeid=#{param1}
	</select>
	
</mapper>